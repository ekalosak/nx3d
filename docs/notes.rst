Developer Notes
=====

When modeling, use Blendr 2.8 so you can use the YABEE plugin to export 3D models as .egg files. The .egg plays nicest
with panda3d. The models in src/nx3d/data/ are unit shape with base at origin.
